import React from 'react';
import Form from './Form';
import Section from './Section';

const MainSection = () => {
	return (
		<main>
			<Section />;
			<Form />
		</main>
	);
};

export default MainSection;
